// --- CREATE/UPDATE/DELETE for Items ---
import type {
  Item,
  Lore,
  Species,
  Faction,
  TimelineEvent,
  RAGRelationship,
} from '../../../shared/types/nodeTypes.js';

export async function createItem(item: Item): Promise<Item & { embedding: number[] }> {
  const { items } = await getCollections();
  const embedding = await generateEmbedding(item.name + ' ' + (item.description || ''));
  const itemWithEmbedding = { ...item, embedding };
  await items.insertOne(itemWithEmbedding);
  return itemWithEmbedding;
}

export async function updateItem(id: string, update: Partial<Item>): Promise<boolean> {
  const { items } = await getCollections();
  const result = await items.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteItem(id: string): Promise<boolean> {
  const { items } = await getCollections();
  const result = await items.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Lore ---
export async function createLore(lore: Lore): Promise<Lore & { embedding: number[] }> {
  const { lore: loreCollection } = await getCollections();
  const embedding = await generateEmbedding(lore.title + ' ' + (lore.description || ''));
  const loreWithEmbedding = { ...lore, embedding };
  await loreCollection.insertOne(loreWithEmbedding);
  return loreWithEmbedding;
}

export async function updateLore(id: string, update: Partial<Lore>): Promise<boolean> {
  const { lore: loreCollection } = await getCollections();
  const result = await loreCollection.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteLore(id: string): Promise<boolean> {
  const { lore: loreCollection } = await getCollections();
  const result = await loreCollection.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Species ---
export async function createSpecies(species: Species): Promise<Species & { embedding: number[] }> {
  const { species: speciesCollection } = await getCollections();
  const embedding = await generateEmbedding(species.name + ' ' + (species.description || ''));
  const speciesWithEmbedding = { ...species, embedding };
  await speciesCollection.insertOne(speciesWithEmbedding);
  return speciesWithEmbedding;
}

export async function updateSpecies(id: string, update: Partial<Species>): Promise<boolean> {
  const { species: speciesCollection } = await getCollections();
  const result = await speciesCollection.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteSpecies(id: string): Promise<boolean> {
  const { species: speciesCollection } = await getCollections();
  const result = await speciesCollection.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Factions ---
export async function createFaction(faction: Faction): Promise<Faction & { embedding: number[] }> {
  const { factions } = await getCollections();
  const embedding = await generateEmbedding(faction.name + ' ' + (faction.description || ''));
  const factionWithEmbedding = { ...faction, embedding };
  await factions.insertOne(factionWithEmbedding);
  return factionWithEmbedding;
}

export async function updateFaction(id: string, update: Partial<Faction>): Promise<boolean> {
  const { factions } = await getCollections();
  const result = await factions.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteFaction(id: string): Promise<boolean> {
  const { factions } = await getCollections();
  const result = await factions.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Timeline Events ---
export async function createTimelineEvent(
  event: TimelineEvent
): Promise<TimelineEvent & { embedding: number[] }> {
  const { timeline_events } = await getCollections();
  const embedding = await generateEmbedding(event.title + ' ' + (event.description || ''));
  const eventWithEmbedding = { ...event, embedding };
  await timeline_events.insertOne(eventWithEmbedding);
  return eventWithEmbedding;
}

export async function updateTimelineEvent(
  id: string,
  update: Partial<TimelineEvent>
): Promise<boolean> {
  const { timeline_events } = await getCollections();
  const result = await timeline_events.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteTimelineEvent(id: string): Promise<boolean> {
  const { timeline_events } = await getCollections();
  const result = await timeline_events.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Relationships ---
export async function createRelationshipNode(rel: RAGRelationship): Promise<RAGRelationship> {
  const { relationships } = await getCollections();
  await relationships.insertOne(rel);
  return rel;
}

export async function updateRelationshipNode(
  id: string,
  update: Partial<RAGRelationship>
): Promise<boolean> {
  const { relationships } = await getCollections();
  const result = await relationships.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteRelationshipNode(id: string): Promise<boolean> {
  const { relationships } = await getCollections();
  const result = await relationships.deleteOne({ id });
  return result.deletedCount > 0;
}
// ai-server/src/repositories/ragNodeRepository.ts
// Repository for fetching and persisting RAG nodes (universes, books, chapters, characters)
// TODO: Replace with real MongoDB/Mongoose logic

import type { Universe, Book, Chapter, Character } from '../../../shared/types/nodeTypes.js';
import { generateEmbedding } from '../services/embeddingService.js';
import { getCollections } from '../config/database.config.js';
import { ObjectId } from 'mongodb';
// import only once above

// --- CREATE/UPDATE/DELETE for Chapters (embedded in books or separate collection) ---
/**
 * Creates a new chapter in a book, optionally at a specific position.
 * @param bookId - The ID of the book.
 * @param chapter - The chapter object to create.
 * @param position - Optional index to insert the chapter at. If omitted, appends to the end.
 * @returns The created Chapter object.
 *
 * Note: To reorder chapters, create a new chapter at the desired position and delete the original.
 */
export async function createChapter(
  bookId: string,
  chapter: Chapter,
  position?: number
): Promise<Chapter> {
  const { universes } = await getCollections();
  // Generate embedding for chapter content
  const embedding = await generateEmbedding(chapter.title + ' ' + (chapter.description || ''));
  // Ensure chapter has a unique id
  let chapterId = chapter.id;
  if (!chapterId || typeof chapterId !== 'string' || !chapterId.startsWith('chp-')) {
    const base64Title = Buffer.from(chapter.title.trim()).toString('base64').replace(/=+$/, '');
    chapterId = `chp-${base64Title}-${Date.now()}`;
  }
  const chapterWithEmbedding = { ...chapter, id: chapterId, bookId, embedding };
  // Find the universe containing the book
  const universe = await universes.findOne({ 'books.id': bookId });
  if (!universe) throw new Error('Book not found');
  // Find the book and insert the chapter at the specified position
  const books = (universe.books as Book[]).map(book => {
    if (book.id === bookId) {
      const chapters: Chapter[] = Array.isArray(book.chapters) ? [...book.chapters] : [];
      if (typeof position === 'number' && position >= 0 && position <= chapters.length) {
        chapters.splice(position, 0, chapterWithEmbedding);
      } else {
        chapters.push(chapterWithEmbedding);
      }
      return { ...book, chapters };
    }
    return book;
  });
  await universes.updateOne({ _id: universe._id }, { $set: { books } });
  return chapterWithEmbedding;
}

export async function updateChapter(
  bookId: string,
  chapterId: string,
  update: Partial<Chapter>
): Promise<boolean> {
  const { universes } = await getCollections();
  // Find the universe containing the book
  const universe = await universes.findOne({ 'books.id': bookId });
  if (!universe) return false;
  // Update the chapter in the book
  const books = (universe.books as Book[]).map(book => {
    if (book.id === bookId) {
      const chapters = Array.isArray(book.chapters)
        ? book.chapters.map((chap: Chapter) =>
            chap.id === chapterId ? { ...chap, ...update, id: chapterId, bookId } : chap
          )
        : [];
      return { ...book, chapters };
    }
    return book;
  });
  const result = await universes.updateOne({ _id: universe._id }, { $set: { books } });
  return result.modifiedCount > 0;
}

export async function deleteChapter(bookId: string, chapterId: string): Promise<boolean> {
  const { universes } = await getCollections();
  // Find the universe containing the book
  const universe = await universes.findOne({ 'books.id': bookId });
  if (!universe) return false;
  // Remove the chapter from the book
  const books = (universe.books as Book[]).map(book => {
    if (book.id === bookId) {
      const chapters = Array.isArray(book.chapters)
        ? book.chapters.filter((chap: Chapter) => chap.id !== chapterId)
        : [];
      return { ...book, chapters };
    }
    return book;
  });
  const result = await universes.updateOne({ _id: universe._id }, { $set: { books } });
  return result.modifiedCount > 0;
}

// --- FETCH ---
export async function fetchUniverses(): Promise<Universe[]> {
  const { universes } = await getCollections();
  const docs = await universes.find({}).toArray();
  return docs.map(doc => ({
    id: doc.id || doc._id?.toString(),
    title: doc.title,
    type: doc.type || 'universe',
    summary: doc.summary ?? '',
    lore: doc.lore ?? '',
    rules: doc.rules ?? '',
    metadata: doc.metadata ?? {},
    embedding: doc.embedding,
    // Include any other fields present in the document
    ...Object.fromEntries(
      Object.entries(doc).filter(
        ([k]) =>
          ![
            'id',
            '_id',
            'title',
            'type',
            'summary',
            'lore',
            'rules',
            'metadata',
            'embedding',
          ].includes(k)
      )
    ),
  }));
}

/**
 * Fetches all books for a given universe, including chapters.
 * @param universeId - The ID of the universe.
 * @returns Array of Book objects, each with chapters if present.
 */
export async function fetchBooks(universeId: string): Promise<Book[]> {
  const { universes } = await getCollections();
  const universe = await universes.findOne({ id: universeId });
  if (universe && Array.isArray(universe.books)) {
    return universe.books.map((doc: any) => ({
      id: doc.id || doc._id?.toString(),
      universeId,
      title: doc.title,
      type: 'book',
      metadata: doc.metadata,
      chapters: Array.isArray(doc.chapters) ? doc.chapters : [],
    }));
  }
  return [];
}

/**
 * Fetches all chapters for a given book.
 * @param bookId - The ID of the book.
 * @returns Array of Chapter objects.
 */
export async function fetchChapters(bookId: string): Promise<Chapter[]> {
  const { universes } = await getCollections();
  const universe = await universes.findOne({ 'books.id': bookId });
  if (!universe || !Array.isArray(universe.books)) return [];
  const book = universe.books.find((b: any) => b.id === bookId);
  return Array.isArray(book?.chapters) ? book.chapters : [];
}

/**
 * Fetches all characters matching the query.
 * @param query - Query object with universeId, bookId, or chapterId.
 * @returns Array of Character objects.
 */
export async function fetchCharacters(query: {
  universeId?: string;
  bookId?: string;
  chapterId?: string;
}): Promise<Character[]> {
  const { generated_characters } = await getCollections();
  const filter: any = {};
  if (query.universeId) filter.universeId = query.universeId;
  if (query.bookId) filter.appearanceBookIds = query.bookId;
  if (query.chapterId) filter.appearanceChapterIds = query.chapterId;
  const docs = await generated_characters.find(filter).toArray();
  return docs.map(doc => ({
    id: doc.id || doc._id?.toString(),
    universeId: doc.universeId,
    name: doc.name,
    type: 'character',
    aliases: doc.aliases || [],
    description: doc.description || '',
    appearanceBookIds: doc.appearanceBookIds || [],
    appearanceChapterIds: doc.appearanceChapterIds || [],
    appearanceSectionIds: doc.appearanceSectionIds || [],
    metadata: doc.metadata || {},
  }));
}

// --- CREATE/UPDATE/DELETE for Universes ---
export async function createUniverse(universe: Universe): Promise<Universe> {
  const { universes } = await getCollections();
  // Generate embedding for universe
  const embedding = await generateEmbedding(
    universe.title + ' ' + (universe.metadata?.description || '')
  );
  const universeWithEmbedding = { ...universe, embedding };
  await universes.insertOne(universeWithEmbedding);
  return universeWithEmbedding;
}

export async function updateUniverse(id: string, update: Partial<Universe>): Promise<boolean> {
  const { universes } = await getCollections();
  // Try update by id (string)
  let result = await universes.updateOne({ id }, { $set: update });
  if (result.modifiedCount > 0) return true;
  // Try update by _id (ObjectId)
  let objectId: ObjectId | undefined;
  if (ObjectId.isValid(id)) {
    objectId = new ObjectId(id);
    result = await universes.updateOne({ _id: objectId }, { $set: update });
    if (result.modifiedCount > 0) return true;
  }
  return false;
}

export async function deleteUniverse(id: string): Promise<boolean> {
  const { universes } = await getCollections();
  // Try delete by id (string)
  let result = await universes.deleteOne({ id });
  if (result.deletedCount > 0) return true;
  // Try delete by _id (ObjectId)
  let objectId: ObjectId | undefined;
  if (ObjectId.isValid(id)) {
    objectId = new ObjectId(id);
    result = await universes.deleteOne({ _id: objectId });
    if (result.deletedCount > 0) return true;
  }
  return false;
}

// --- CREATE/UPDATE/DELETE for Books (embedded in universes or separate collection) ---
export async function createBook(universeId: string, book: Book): Promise<Book> {
  const { universes } = await getCollections();
  // Generate embedding for book
  const embedding = await generateEmbedding(book.title + ' ' + (book.metadata?.description || ''));
  const bookWithEmbedding = { ...book, embedding };
  await universes.updateOne({ id: universeId }, [
    {
      $set: {
        books: {
          $cond: [
            { $isArray: '$books' },
            { $concatArrays: ['$books', [bookWithEmbedding]] },
            [bookWithEmbedding],
          ],
        },
      },
    },
  ]);
  return bookWithEmbedding;
}

export async function updateBook(
  universeId: string,
  bookId: string,
  update: Partial<Book>
): Promise<boolean> {
  const { universes } = await getCollections();
  const result = await universes.updateOne(
    { id: universeId, 'books.id': bookId },
    { $set: { 'books.$': { ...update, id: bookId, universeId } } }
  );
  return result.modifiedCount > 0;
}

export async function deleteBook(universeId: string, bookId: string): Promise<boolean> {
  const { universes } = await getCollections();
  const result = await universes.updateOne({ id: universeId }, [
    {
      $set: {
        books: {
          $cond: [
            { $isArray: '$books' },
            {
              $filter: {
                input: '$books',
                as: 'book',
                cond: { $ne: ['$$book.id', bookId] },
              },
            },
            [],
          ],
        },
      },
    },
  ]);
  return result.modifiedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Locations ---
import type { Location } from '../../../shared/types/nodeTypes.js';

export async function createLocation(
  location: Location
): Promise<Location & { embedding: number[] }> {
  const { locations } = await getCollections();
  // Generate embedding for location
  const embedding = await generateEmbedding(location.name + ' ' + (location.description || ''));
  const locationWithEmbedding = { ...location, embedding };
  await locations.insertOne(locationWithEmbedding);
  return locationWithEmbedding;
}

export async function updateLocation(id: string, update: Partial<Location>): Promise<boolean> {
  const { locations } = await getCollections();
  const result = await locations.updateOne({ id }, { $set: update });
  return result.modifiedCount > 0;
}

export async function deleteLocation(id: string): Promise<boolean> {
  const { locations } = await getCollections();
  const result = await locations.deleteOne({ id });
  return result.deletedCount > 0;
}

// --- CREATE/UPDATE/DELETE for Characters ---
export async function createCharacter(character: Character): Promise<Character> {
  const { generated_characters } = await getCollections();
  // Generate embedding for character
  const embedding = await generateEmbedding(character.name + ' ' + (character.description || ''));
  const canonicalCharacter: Character & { embedding: number[] } = {
    ...character,
    embedding,
  };
  await generated_characters.insertOne(canonicalCharacter);
  return canonicalCharacter;
}

export async function updateCharacter(id: string, update: Partial<Character>): Promise<boolean> {
  const { generated_characters } = await getCollections();
  const allowedFields = [
    'name',
    'title',
    'aliases',
    'description',
    'appearanceBookIds',
    'appearanceChapterIds',
    'appearanceSectionIds',
    'metadata',
    'pronounLinks',
    'contextWindow',
    'sourceReferences',
  ];
  const canonicalUpdate: Partial<Character> = {};
  for (const key of allowedFields) {
    if (key in update) {
      (canonicalUpdate as any)[key] = (update as any)[key];
    }
  }
  const result = await generated_characters.updateOne({ id }, { $set: canonicalUpdate });
  return result.modifiedCount > 0;
}

export async function deleteCharacter(id: string): Promise<boolean> {
  const { generated_characters } = await getCollections();
  const result = await generated_characters.deleteOne({ id });
  return result.deletedCount > 0;
}

/**
 * Scans all node collections and removes nodes whose universeId does not exist in universes.
 * Should be called periodically (e.g., via a cron job or setInterval).
 * @returns Number of deleted orphaned nodes.
 */
export async function cleanupOrphanedNodesPeriodic(): Promise<number> {
  const { universes, generated_characters, locations, items, species, factions, timeline_events } =
    await getCollections();
  const universeIds = new Set(
    (await universes.find({}, { projection: { id: 1 } }).toArray()).map(u => u.id)
  );
  let totalDeleted = 0;

  const collections = [generated_characters, locations, items, species, factions, timeline_events];

  for (const collection of collections) {
    // Find orphaned nodes
    const orphaned = await collection
      .find({ universeId: { $nin: Array.from(universeIds) } })
      .toArray();
    if (orphaned.length > 0) {
      const ids = orphaned.map(n => n.id);
      const result = await collection.deleteMany({ id: { $in: ids } });
      totalDeleted += result.deletedCount ?? 0;
    }
  }
  return totalDeleted;
}

// Example: Schedule periodic cleanup (every 24 hours)
// Uncomment the following block to enable automatic cleanup
/*
setInterval(() => {
    cleanupOrphanedNodesPeriodic()
        .then(count => {
            if (count > 0) {
                console.log(`[Orphaned Node Cleanup] Removed ${count} orphaned nodes.`);
            }
        })
        .catch(err => console.error('[Orphaned Node Cleanup] Error:', err));
}, 24 * 60 * 60 * 1000); // 24 hours
*/
